import { getSettings } from '@/lib/data';
export default async function SettingsAdmin(){const s=await getSettings() as Record<string, string>;const fields=['siteName','logoUrl','primaryColor','secondaryColor','heroTitle','heroText','heroImage','promoTitle','promoText','footerText','contactEmail','contactPhone','contactAddress','seoTitle','seoDescription'];return <main><h1 className="text-3xl font-black">Site settings</h1><form action="/api/settings" method="post" className="card mt-6 grid gap-4 p-6">{fields.map(f=><label className="grid gap-2" key={f}><span className="label">{f}</span>{f.toLowerCase().includes('text')||f.toLowerCase().includes('description')?<textarea name={f} className="input" defaultValue={s[f]}/>:<input name={f} className="input" defaultValue={s[f]}/>}</label>)}<button className="btn-primary rounded-2xl py-3 font-bold">Save settings</button></form></main>}
