import Header from '@/components/Header'; import Footer from '@/components/Footer'; import { getSettings } from '@/lib/data'; import Link from 'next/link';
export default async function CartPage(){const settings=await getSettings();return <><Header settings={settings}/><main className="mx-auto max-w-4xl px-4 py-16"><div className="card p-8 text-center"><h1 className="text-3xl font-black">Your cart</h1><p className="mt-3 text-slate-600">Cart UI is ready for practice. Connect local storage or a payment provider next.</p><Link href="/checkout" className="btn-primary mt-6 inline-block rounded-full px-6 py-3 font-bold">Checkout</Link></div></main><Footer settings={settings}/></>}
