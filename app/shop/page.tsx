import Header from '@/components/Header'; import Footer from '@/components/Footer'; import ProductCard from '@/components/ProductCard'; import { getProducts, getSettings } from '@/lib/data';
export default async function ShopPage() { const [settings, products] = await Promise.all([getSettings(), getProducts()]); return <><Header settings={settings}/><main className="mx-auto max-w-7xl px-4 py-12"><h1 className="text-4xl font-black">Shop</h1><p className="mt-3 text-slate-600">Browse all products managed from MongoDB.</p><div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((p:any)=><ProductCard key={p._id} product={p}/>)}</div></main><Footer settings={settings}/></>; }
