import { NextResponse } from 'next/server'; import { connectDB } from '@/lib/mongodb'; import Product from '@/models/Product'; import { requireAdmin } from '@/lib/auth'; import { slugify } from '@/lib/utils';
export async function POST(request: Request,{params}:{params:{id:string}}){requireAdmin();const f=await request.formData();await connectDB();await Product.findByIdAndUpdate(params.id,{name:f.get('name'),slug:slugify(String(f.get('name'))),image:f.get('image'),price:Number(f.get('price')),discountPrice:Number(f.get('discountPrice'))||undefined,description:f.get('description'),category:f.get('category'),stock:Number(f.get('stock')),featured:f.get('featured')==='on'});return NextResponse.redirect(new URL('/admin/products',request.url));}
