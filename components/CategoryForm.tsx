export default function CategoryForm({ category }: { category?: any }) { const action = category?._id ? `/api/categories/${category._id}` : '/api/categories'; return <form action={action} method="post" className="card mt-6 grid gap-4 p-6"><input name="name" className="input" placeholder="Category name" defaultValue={category?.name}/><input name="image" className="input" placeholder="Image URL" defaultValue={category?.image}/><textarea name="description" className="input min-h-28" placeholder="Description" defaultValue={category?.description}/><button className="btn-primary rounded-2xl py-3 font-bold">Save category</button>{category?._id ? <button formAction={`/api/categories/${category._id}/delete`} className="rounded-2xl bg-red-600 py-3 font-bold text-white">Delete category</button> : null}</form>; }
