export default function ProductForm({ product, categories }: { product?: any; categories: any[] }) { const action = product?._id ? `/api/products/${product._id}` : '/api/products'; return <form action={action} method="post" className="card mt-6 grid gap-4 p-6"><input name="name" className="input" placeholder="Product name" defaultValue={product?.name}/><input name="image" className="input" placeholder="Image URL" defaultValue={product?.image}/><div className="grid gap-4 md:grid-cols-2"><input name="price" type="number" step="0.01" className="input" placeholder="Price" defaultValue={product?.price}/><input name="discountPrice" type="number" step="0.01" className="input" placeholder="Discount price" defaultValue={product?.discountPrice}/></div><select name="category" className="input" defaultValue={product?.category?._id || product?.category}>{categories.map(c=><option key={c._id} value={c._id}>{c.name}</option>)}</select><textarea name="description" className="input min-h-32" placeholder="Description" defaultValue={product?.description}/><div className="grid gap-4 md:grid-cols-2"><input name="stock" type="number" className="input" placeholder="Stock" defaultValue={product?.stock}/><label className="flex items-center gap-3 rounded-2xl border p-3"><input name="featured" type="checkbox" defaultChecked={product?.featured}/> Featured product</label></div><button className="btn-primary rounded-2xl py-3 font-bold">Save product</button>{product?._id ? <button formAction={`/api/products/${product._id}/delete`} className="rounded-2xl bg-red-600 py-3 font-bold text-white">Delete product</button> : null}</form>; }
